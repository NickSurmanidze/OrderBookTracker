# Server Configuration
PORT=4000

# CORS Configuration
# Comma-separated list of allowed origins for production
# Example: CORS_ORIGIN=https://yourapp.com,https://www.yourapp.com
CORS_ORIGIN=http://localhost:3000

# WebSocket Broadcast Configuration
# Interval in milliseconds for broadcasting market data to clients
# Lower values = more frequent updates but higher server load
# Recommended: 300-1000ms
BROADCAST_INTERVAL_MS=500

# Kraken WebSocket URL (optional, defaults to production)
# KRAKEN_WS_URL=wss://ws.kraken.com/v2
